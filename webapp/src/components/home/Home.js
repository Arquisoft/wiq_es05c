import React from 'react';
import StartButton from '../startbutton/StartButton';

const Home = () => {
  return (
    <div>

   
      <StartButton isLoggedIn={true} />

    </div>
  );
};

export default Home;